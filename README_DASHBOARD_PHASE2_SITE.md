# Store Bot Dashboard Phase 2 Site Patch

This patch replaces `dashboard.html` with the Phase 2 dashboard UI.

New UI features:

- Better internal-server-error display.
- Phase 2 server maintenance controls.
- Enable/disable server maintenance directly from the dashboard.
- Keeps product editing and AutoPay editing locked until later phases.
